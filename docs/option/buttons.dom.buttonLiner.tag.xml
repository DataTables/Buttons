<?xml version="1.0" encoding="UTF-8" ?>
<dt-option group="core">
	<name>buttons.dom.buttonLiner.tag</name>
	<summary>Set the HTML tag to use for the elements used to create individual button</summary>
	<since>1.0.0</since>

	<default value="a"/>

	<type type="string">
		<description>
			The HTML tag to use. There should be no spaces or any other formatting - e.g. it should simply be `span`, `div`, etc.
		</description>
	</type>

	<type type="null">
		<description>
			When set to `null`, this option will disable the button liner element and the buttons will be created without a liner.
		</description>
	</type>

	<description>
	<![CDATA[
		This option controls the HTML tag that is used as the liner for each button. This can be particularly useful for adding complex styling rules to buttons. It can also be disabled if you wish to have minimal markup in your document.

		Please refer to the `b-init buttons.dom` documentation for an overview of the DOM structure that Buttons will create. In this specific case, this option controls the `<span>...</span>` button liner element that is shown in the example HTML there.
	]]>
	</description>

	<example title="DataTables initialisation: Use an `div` tag for the button liner"><![CDATA[

$('#myTable').DataTable( {
	buttons: {
		dom: {
			buttonLiner: {
				tag: 'div'
			}
		}
	}
} );

]]></example>

	<example title="Instance initialisation: Disable the button liner"><![CDATA[

new $.fn.dataTable.Buttons( table, {
	buttons: {
		dom: {
			buttonLiner: {
				tag: null
			}
		}
	]
} );

]]></example>
</dt-option>